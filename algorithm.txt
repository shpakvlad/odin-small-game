SET comp = 0
SET user = 0

PRINT rules

FOR i=0; i<5; i++
    REPEAT 
        SET user READ 
        SET comp CALL getCompVariant RETURNING random(1, 3)
        IF user == comp PRINT error
    UNTIL comp != user 

    IF comp = rock AND user = scissors THEN INCREMENT comp
    ELSE IF comp = rock AND user = paper THEN INCREMENT user
    ELSE IF comp = scissors AND user = rock THEN INCREMENT user
    ELSE IF comp = scissors AND user = 3 THEN INCREMENT comp
    ELSE IF comp = paper AND user = rock THEN INCREMENT comp
    ELSE IF comp = paper AND user = scissors THEN INCREMENT user  
ENDFOR

PRINT result

